[project]
name = "hackathon"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "username", email = "nevis.rumi@us.af.mil"},
]
dependencies = ["asksageclient>=1.42", "ruff>=0.14.2"]
requires-python = "==3.12.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
